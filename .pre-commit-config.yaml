repos:
  - repo: local
    hooks:
      - id: uv-lock
        name: uv lock
        entry: bash -c 'uv lock'
        language: system
        files: ^pyproject\.toml$
        pass_filenames: false
        stages: [commit]
